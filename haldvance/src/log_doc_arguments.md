- `[($predicate)]?`: The condition under which to log something,
  pass `(true)` or elide it to log unconditionally.
- `$format_string [, $format_args]*` format arguments, the same syntax
  as the standard library `println!`.

